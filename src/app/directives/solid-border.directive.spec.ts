import { SolidBorderDirective } from './solid-border.directive';

describe('SolidBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new SolidBorderDirective();
    expect(directive).toBeTruthy();
  });
});
