import { AppRoundBlockDirective } from './app-round-block.directive';

describe('AppRoundBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new AppRoundBlockDirective();
    expect(directive).toBeTruthy();
  });
});
